
import { reviews } from 'message-board/helpers/reviews';
import { module, test } from 'qunit';

module('Unit | Helper | reviews');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reviews([42]);
  assert.ok(result);
});

